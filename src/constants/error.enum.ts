export enum ERROR_CODE {
  NO_ROOM = 901,
  LANGUAGE_CODE_NOT_FOUND = 902,
}

export enum ERROR_MESSAGE {
  NO_ROOM = 'Client have not joined a room',
  LANGUAGE_CODE_NOT_FOUND = 'Client native language code not registered',
}
